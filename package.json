{
  "dependencies": {
    "mobx-state-tree": "^5.2.0",
    "puppeteer": "^21.3.7"
  },
  "devDependencies": {
    "webpack": "^5.88.2",
    "webpack-cli": "^5.1.4"
  },
  "scripts": {
    "build": "rm -rf ./build/node/* && webpack --config webpack.config.js",
    "test:node": "npm run build && ./benchmark-node.sh",
    "test:hermes": "npm run build && ./benchmark-hermes.sh",
    "test:web": "npm run build && node browser-tests.js",
    "test": "npm run test:node && npm run test:hermes && npm run test:web"
  }
}
